# --- Core (training + inference) ---
numpy>=1.24,<3.0
pandas>=2.0,<3.0
pyarrow>=12.0
scikit-learn>=1.3,<2.0
joblib>=1.3
PyYAML>=6.0

# --- Serving (FastAPI microservice) ---
fastapi>=0.104
uvicorn[standard]>=0.24
pydantic>=2.0

# --- ONNX export / runtime (optional; joblib remains the fallback) ---
skl2onnx>=1.16
onnx>=1.15
onnxruntime>=1.16

# --- Optional analytics / reporting (events, maps) ---
folium>=0.15

# --- Dev / test ---
pytest>=7.4
httpx>=0.25
